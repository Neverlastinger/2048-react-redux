import React from 'react';
import ReactDOM from 'react-dom';
import './css/index.css';
import Root from './js/components/Root';

/**
 * Renders the root of the application in the #app element.
 */
ReactDOM.render(
    <Root />,
    document.getElementById('root')
);
